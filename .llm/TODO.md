## TODO LIST

### Bugfixes

- [ ] 修复若干样式问题

请把网站标题从 「signin app」 换成「畅课签到助手」

现在扫码完成文本框与对号图标没有在水平上对齐。
设置页修改更新按钮应该保证在宽度较小时，文字不换行。
查看 GitHub 仓库按钮应该改名为 查看 Web 应用仓库。
新增一个按钮在 查看 Web 应用仓库下面，名为「查看 API 端点仓库」。URL 应该从 `/backend/repo/url` 拿到。

- [ ] 修复扫码历史分页问题（前后端补传 `index`）

两个获取历史的接口都应该补传 index（从 0 开始），表示当前是第几页。

- [ ] 修复「仅显示我的签到」单选框

这个框目前没生效，不确定是前端还是后端的原因，排查一下

- [ ] loading 现在不能正常显示

扫码后的 loading 不能正常显示，排查一下。

- [ ] 创建用户时 cookie 非必填，可以让用户以后再填

这个很简单，改一下就好了。没填 cookie 时不用调 refresh 接口。

### Features

- [ ] API 端点输入框开后门

如果输入框输入「613614」，则将 value 设为 https://api.codenebula.top/tronclass
